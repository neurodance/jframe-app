import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'JFrame - Think it. Jott it. Share it.',
  description: 'Create brilliant Jotts in seconds with AI-powered Adaptive Cards',
  keywords: 'jott, adaptive cards, ai, content creation, visual communication',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}